@model IEnumerable<parneetkaur.Models.Dovebathproduct>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DoveSoap)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DoveShampoo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DoveConditioner)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DoveSoap)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DoveShampoo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DoveConditioner)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.C_Giftcardno_ }) |
            @Html.ActionLink("Details", "Details", new { id=item.C_Giftcardno_ }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.C_Giftcardno_ })
        </td>
    </tr>
}

</table>
